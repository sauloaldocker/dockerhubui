// JavaScript File

function init() {
    console.log('initing');
    
    for ( var un in REPOSITORY_USERS ) {
        var username = REPOSITORY_USERS[un];
        console.log("checking user", username);
        //REPOSITORY_REPOS
    }
}
